home udefined slice on first login

layout:  // token != "null" &&

rename routing to route

layout: userinsystem and is token is the same

layout: rename filter vacations

editUser: check comment ---> same in EditVacation in function editvacation

navMenu:   const open = Boolean(anchorEl); ---> why????

home: pass function handlefollowvacation as arrow func with arguments instead of calling this function in media card with args from props

vacation-controller: remove getAllVacations function

add if no vacations than default text

user settings: add option to add user image

reports: add pie chart to display ratio finished this year to future vacations this year

edit user, edit vacation, add vacation: fix top indentation

edit user: default image at registration,

edit user: when updating user it gives him roleId 2